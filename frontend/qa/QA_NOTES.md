# 프론트엔드 시각 QA 기록 (W6)

대상: `frontend/{index.html, styles.css, app.js}`
방법: Playwright(Chromium) 실제 구동 → 1440×900 데스크톱 / 390×844 모바일 전체 페이지 캡처 →
PNG 육안 확인 → 수정 → 재캡처. 총 5회 반복.

재현:
```
cd frontend/qa
python qa_shot.py final          # 백엔드 기동 상태. 기본 http://127.0.0.1:8000 (scripts/dev.bat)
                                 # 다른 포트면 QA_URL 로 준다
python qa_offline.py             # file:// 로 열어 /api/* 전면 차단 → 로컬 판정 경로 검증
QA_URL=http://127.0.0.1:8000/ python qa_offline.py   # 같은 오리진에서 서빙하되 /api/* 만 차단
```

`qa_shot.py` 는 보고서를, 둘 다 스크린샷을 이 디렉터리에 떨군다. **둘 다 `.gitignore` 대상이다** —
커밋된 실행 산출물은 코드가 바뀌면 조용히 거짓이 된다.

---

## 육안으로 발견해 고친 시각 결함

| # | 결함 (직접 보고 확인) | 원인 | 수정 |
|---|---|---|---|
| 1 | **모바일 가로 스크롤** — 390px 뷰포트에서 문서 폭이 503px. 전 페이지가 잘려 보임 | 상단바의 브랜드+연결배지+버튼이 358px 안에 안 들어감 | ≤620px에서 연결배지를 색상 점으로 축약. 문구는 `title`/`aria-label`로 유지 |
| 2 | **적합도 도넛 링 찌그러짐** — 40px 링이 카드 폭만큼 늘어나 찌그러진 호로 렌더 | `.viz { width:100% }`가 고정 크기 SVG에도 적용됨 | 고정 크기 전용 `.fit-ring` 클래스 분리 |
| 3 | **게이지 마커가 반토막 글리프처럼 보임** | 구간 경계 흰 눈금선을 마커 **뒤에** 그려서 마커 원을 관통 | 눈금선 → 값 호 → 마커 순으로 레이어 재정렬 |
| 4 | **게이지 "25%" 라벨과 마커 충돌** — 슈바베지수가 정확히 25.0%일 때 겹침 | 라벨 반경 부족 | 반경 +3px, 라벨에 흰색 후광(`paint-order: stroke`) |
| 5 | **TCO 막대 라벨 잘림** — "월세 — 지역 표준 보증금 + 중소기업취업청년 전월세보증금대출"이 모바일에서 중간 절단 | `white-space: nowrap` 단일 행 | 최대 2행 래핑 후 말줄임 |
| 6 | **시나리오 카드 숫자 줄 어긋남** — 제목 1행/2행 차이로 "81만"과 "99만"이 20px 어긋나 좌우 비교가 안 됨 | 카드마다 독립 높이 | `grid-template-rows: subgrid`로 행 트랙 공유 (`@supports` 가드) |
| 7 | **TCO 축 눈금 위치 어긋남** — 가운데 눈금이 25/50/75% 격자선과 불일치 | `justify-content: space-between` | 눈금을 실제 %에 절대 배치 |
| 8 | **폼 CTA 도달 불가** — 결과를 보며 스크롤하면 sticky 사이드바(1375px)의 하단(주거 조건 스위치 + 진단 버튼)이 영구히 화면 밖 | `top`만 지정, 뷰포트보다 큰 요소 | 사이드바에 `max-height` + 자체 스크롤 |
| 9 | **연결 배지 오표시** — 백엔드가 OpenAI로 동작 중인데 "템플릿 응답"으로 표기 | `h.llm === 'live'` 비교. 계약(§5)상 값은 `openai\|anthropic\|offline` | `offline`이 아니면 라이브로 판정 |
| 10 | **대비 부족 다수 (WCAG AA 미달)** — 아이브로우/범례/단위/도움말/푸터 등 | `--ink-3 #8B857B` 3.3:1, `--ink-4 #ADA69A` 2.05:1 | `--ink-3 #655F53`(5.4~6.3:1), `--ink-4 #6F6A5D`(4.6~5.4:1)로 조정 |
| 11 | 오프라인에서 동작마다 4.5초 정지 + 실패 요청 반복 | 백엔드 부재 확인 후에도 매번 재호출 | health 실패 확정 시 이후 호출 즉시 단락 |

## 최종 검증 결과 (W6 당시)

- **콘솔 에러 0** — 데스크톱/모바일, 온보딩·대시보드·채팅 전 구간.
- **가로 스크롤 없음** — 데스크톱 1440/1440, 모바일 390/390.
- **대비** — 렌더된 모든 텍스트가 AA 통과. (그라디언트 배경 카드는 스크립트가 측정 불가로 제외하며, 수동 계산 결과 요약 카드 본문 15.5:1 / 메타 5.0:1로 통과)
- **흐름** — 폼 입력 → 진단 → 대시보드(시나리오 4 · 정책 9~10 · 리스크) → 채팅 1턴/2턴 모두 정상. 전송 버튼 busy 중 비활성, 완료 후 복구.
- **오프라인 폴백** — `/api/*` 전면 차단 시 "데모 모드 · 내장 엔진"으로 전환, 지역 9개·시나리오 4개·정책 9개·리스크 41 정상 렌더, 템플릿 채팅 응답 동작.

## 남은 리스크

- 오프라인 시 부팅 프로브(health·regions) 2건이 브라우저 네트워크 레벨 로그를 남깁니다.
  앱은 모두 처리하며, 백엔드 부재를 탐지하려면 요청은 불가피합니다. 앱 예외는 0건입니다.
- 채팅 모드 칩이 실행마다 "LLM 라이브"/"결정론적 템플릿"으로 갈립니다. 백엔드가 반환한
  `mode` 값을 그대로 반영한 것으로, OpenAI 호출 실패 시 폴백이 작동한 결과입니다(프론트 결함 아님).
- `subgrid` 미지원 구형 브라우저는 `@supports`로 기존 flex 레이아웃을 사용합니다(정렬만 완화, 파손 없음).

---

## 2026-08-15 · 위 기록 중 지금 화면과 다른 것 (W-demo-assets)

위 표는 **W6 시점의 사실**이고 그대로 둔다. 그러나 PR #67(6단계 · F-1)이 화면을 갈아끼웠으므로
아래 항목은 **더 이상 지금 화면을 설명하지 않는다.** 표시 없이 두면 다음 사람이 그것을 믿는다.

| 위 기록 | 지금 | 근거 |
|---|---|---|
| 결함 3·4 (게이지 마커 레이어 · "25%" 라벨 충돌) | **해당 없음.** 반원 게이지가 삭제됐다. `gaugeSVG()`·`polar()`·`arcPath()` 도 함께 지워졌고, 그 자리는 시나리오별 실측 막대다 | PR #67 ②③ |
| 결함 4 의 전제 "슈바베지수가 정확히 25.0%" | `affordability.schwabeIndexPct` 는 계약에서 제거됐다. 슈바베지수는 **시나리오별** 필드로만 남았다 | SPEC 5.2.1 · `contracts/openapi.json` |
| 「오프라인 폴백 — "데모 모드 · 내장 엔진" 전환」 | 배지는 **「백엔드 미연결 · 로컬 판정 경로」**, 그리고 `#localBanner` 가 숫자의 출처를 화면에 적는다. 정적 `MOCK_RESPONSE` 는 삭제됐고, 생성물이 없으면 **판정을 끈다** | PR #67 ⑥ · SPEC 6.2 D-11 |
| 「지역 9개·시나리오 4개·정책 9개·리스크 41」 | 이번 실측(오프라인, 기본 프로필): **지역 10 · 시나리오 4 · 정책 8 · 리스크 32** | `qa_offline.py` 실행 출력 |
| 남은 리스크 ①「부팅 프로브 2건」 | **3건**이다 — `/api/health` · `/api/regions` · `/api/auth/session`. 마지막 것은 4단계 로그인이 추가했다 | `qa_offline.py` 실행 출력 |

여전히 유효한 것: 결함 1·2·5·6·7·8·10 의 수정, 결함 9 의 `llm !== 'offline'` 판정
(`app.js` 의 `checkHealth`), 결함 11 의 로컬 경로 단락(`app.js` 의 `apiFetch`).

### 이번 실행에서 새로 나온 것 (미수정 · `frontend/` 는 W-web 소유)

`python qa_shot.py` 를 백엔드 기동 상태로 돌렸을 때 **콘솔 0건 · 가로 스크롤 없음 · 대비 위반 0건**
이었으나, PR #67 이 새로 그린 시나리오 막대에서 **텍스트 잘림 3건**이 나왔다.

```
CLIP span.schwabe-name | 전세 + 청년전용 버팀목전세자금대출              sw=181 cw=156   (desktop)
CLIP span.schwabe-name | 월세 — 지역 표준 보증금 + 청년전용 보증부월세대출  sw=259 cw=156   (desktop)
CLIP span.schwabe-name | 월세 — 저보증금·고월세형                      sw=129 cw=108   (mobile)
```

`.schwabe-name` 의 이름 칸이 시나리오 라벨보다 좁다. QA_NOTES 결함 5 와 같은 부류이며
(그때는 TCO 막대 라벨), 그때의 해법은 2행 래핑 후 말줄임이었다. **고치지 않았다 —
`frontend/styles.css` 는 이 과업의 소유 경로가 아니다.**
