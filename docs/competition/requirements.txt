# 발표 자료 생성기(`build_ppt.py`)의 의존성. **제품 의존성이 아니다.**
#
# `backend/requirements.txt` 에 넣지 않는 이유 — 그 파일은 심사위원이 제품을 돌리려고
# 설치하는 목록이고, 문서 도구를 섞으면 「이 제품은 pptx 를 필요로 한다」로 읽힌다.
# 제품은 이것 없이 완전히 동작한다.
#
# 그런데 CI 는 이것을 설치한다. 발표 자료가 **생성물**이고(SPEC 9.4 [모든 생성물])
# 그 재생성 대조를 CI 가 지기 때문이다 — PR #76 이 생성기 22곳을 고치고 덱을 다시
# 뽑지 않은 것을 아무도 못 잡은 자리가 정확히 여기다.
python-pptx>=0.6.23

# 공식 제출 PDF 2종 생성 및 최종 산출물 검증.
reportlab>=4.2
pypdf>=5.0
pymupdf>=1.24
playwright>=1.48
